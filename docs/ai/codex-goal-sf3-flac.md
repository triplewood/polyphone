# Codex Goal Prompt: Polyphone SF3+FLAC Support Convergence

Use this prompt to continue from the current Polyphone SF3+FLAC work toward a clean, reviewable, and release-ready state.

```text
Create a Codex Goal for /Users/gary/Work/Yasile/src/polyphone:

Objective: Converge Polyphone SF3+FLAC support into a clean, reviewable, and locally verified implementation without broad rewrites or unrelated cleanup.

Context:
- User confirmed /Applications/Polyphone.app plays ordinary SF2 with sound.
- User confirmed /Applications/Polyphone.app opens /tmp/polyphone-flac.sf3 and SF3+FLAC playback has sound.
- Root cause of the prior all-format silence was missing Voice::prepareTables() initialization in sources/main.cpp, not the FLAC sample decode path.
- Current FLAC-SF3 fixture was generated by /Users/gary/Work/Yasile/src/dream_snddev/tools/sbkit from /Users/gary/Desktop/Xiao_C_sus_V05.sf2 with sf3_codec=flac; RIFF smpl magic is fLaC.
- scripts/smoke_sf3_flac.sh passed and covers SF2, Ogg-SF3, FLAC-SF3, FLAC-SF3 resave preservation, and real sbkit Pipa/ErHu candidate roundtrips.
- Follow AGENTS.md and GitNexus rules. Run impact analysis before symbol edits and detect_changes before final/commit.
- Keep changes small, brownfield, and reviewable. Prefer existing Polyphone architecture. Do not add new dependencies beyond the already-used libsndfile/FLAC stack unless explicitly approved.

Primary files / surfaces:
- sources/core/sample/samplereadersf.cpp: FLAC blob detection and libsndfile virtual IO decode for rawData beginning with fLaC; preserve existing OggS path.
- sources/core/input/sf/sf2sdtapart.cpp: RIFF odd-size smpl padding skip.
- sources/main.cpp: restore Voice::prepareTables() in GUI and selftest startup paths; keep POLYPHONE_SYNTH_SELFTEST as regression verification surface.
- scripts/smoke_sf3_flac.sh: local regression smoke for SF2, Ogg-SF3, FLAC-SF3, and sbkit real candidates.
- docs/ai/sf3-flac-support-design.md: design/verification source of truth.

Required loop:
1. Inspect git status and group current changes into review slices:
   a. audio regression fix,
   b. SF3+FLAC decode support,
   c. macOS arm64 packaging/build support,
   d. other UI/open/playback integration changes.
2. Remove temporary diagnostics or marker-based debug code. Keep only intentional env-gated selftest/logging that supports verification.
3. Verify build:
   make -C sources -j4
4. Verify ordinary SF2 synth path:
   POLYPHONE_SYNTH_SELFTEST=1 POLYPHONE_SYNTH_SELFTEST_FILE=/Users/gary/Desktop/Xiao_C_sus_V05.sf2 ./macos/polyphone.app/Contents/MacOS/polyphone
   Acceptance: command exits 0 and reports non-zero maxAbs.
5. Verify SF3+FLAC synth path:
   If /tmp/polyphone-flac.sf3 is missing, regenerate it with sbkit:
     cd /Users/gary/Work/Yasile/src/dream_snddev
     python3 -m tools.sbkit.main convert /Users/gary/Desktop/Xiao_C_sus_V05.sf2 -o /tmp/polyphone-flac.sf3 --output-format sf3 --input-format sf2 --meta sf3_codec=flac --meta sf3_quality=6 --jobs 4
   Then run:
     POLYPHONE_SYNTH_SELFTEST=1 POLYPHONE_SYNTH_SELFTEST_FILE=/tmp/polyphone-flac.sf3 ./macos/polyphone.app/Contents/MacOS/polyphone
   Acceptance: command exits 0 and reports non-zero maxAbs.
6. Verify broad SF3+FLAC regression:
   bash scripts/smoke_sf3_flac.sh
   Acceptance: prints SF3 FLAC smoke passed.
7. Verify installed app when requested:
   ditto macos/polyphone.app /Applications/Polyphone.app
   file /Applications/Polyphone.app/Contents/MacOS/polyphone
   Acceptance: Mach-O 64-bit executable arm64; user/manual playback remains audible.
8. Run git diff --check and GitNexus detect_changes(scope=all). Report risk and affected processes.
9. Produce a final handoff with changed files, review slices, validation evidence, and remaining release risks. Do not commit/push unless explicitly approved.

Acceptance criteria:
- SF2 playback works and is verified by selftest/manual evidence.
- SF3+FLAC playback works and is verified by selftest/manual evidence.
- Ogg-SF3 behavior is not regressed by smoke.
- FLAC-SF3 resave preserves FLAC compressed sample blobs according to smoke.
- macOS local arm64 app builds and installs locally.
- No temporary marker/debug dump code remains.
- Documentation names remaining risk: Homebrew dylib dependency bundling is not yet release packaging.

Stop when the above evidence is collected or when an external release/signing/commit action requires explicit user approval.
```
