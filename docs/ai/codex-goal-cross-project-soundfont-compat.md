# Codex Goal: Cross-Project SoundFont Compatibility

Use this prompt for an autonomous Codex goal after reviewing the canonical sbkit spec and the Polyphone/EWI consumer notes.

```text
Goal: Implement the next Loop Engineering slice for cross-project SoundFont compatibility across:
1. /Users/gary/Work/Yasile/src/dream_snddev/tools/sbkit
2. /Users/gary/Work/Yasile/src/polyphone
3. /Users/gary/Work/Yasile/src/ewi-midi-synthesizer/package/ewi_midi_synthesizer

Primary objective:
Make SF3/FLAC files generated by sbkit verifiably consumable by both Polyphone and EWI MIDI Synthesizer, using a shared fixture/contract approach rather than private assumptions.

Authoritative producer/spec document:
/Users/gary/Work/Yasile/src/dream_snddev/tools/sbkit/docs/soundfont-compatibility-spec.md

Consumer notes:
/Users/gary/Work/Yasile/src/polyphone/docs/ai/soundfont-cross-project-contract.md
/Users/gary/Work/Yasile/src/ewi-midi-synthesizer/package/ewi_midi_synthesizer/docs/design/SOUND_LIBRARY_COMPATIBILITY.md

Preferred local compatibility gate:
/Users/gary/Work/Yasile/src/polyphone/scripts/smoke_cross_project_soundfont.sh

Constraints:
- Prefer SF2/SF3 as the cross-project interchange formats.
- Treat SFX as versioned product containers; do not assume sbkit SFX1 and EWI SFX2 are mutually compatible.
- Keep the canonical custom-format documentation in sbkit/SDK, not in the Polyphone fork.
- Keep changes small and reversible.
- Do not couple Polyphone or EWI directly to sbkit runtime internals.
- Avoid broad refactors and new dependencies.
- Preserve existing SF2 and SF3/Vorbis behavior.
- Use generated tiny fixtures before real-bank scale tests.
- For Polyphone symbol edits, follow repo GitNexus impact-analysis rules before modifying functions/classes/methods.

Recommended execution loop:
1. Contract check:
   - Read the canonical sbkit spec first: tools/sbkit/docs/soundfont-compatibility-spec.md.
   - Read the Polyphone editor note and EWI runtime policy as consumer documents.
   - Keep format authority in sbkit/SDK; do not define Yasile custom formats in the Polyphone fork.
2. sbkit fixture loop:
   - Add or expose a deterministic tiny fixture generator for SF2, SF3/Vorbis, SF3/FLAC, odd-padding SF3/FLAC, and multi-sample SF3/FLAC.
   - Add fixture metadata with expected codec magic and sample ranges.
   - Extend verify_sf3 if needed to check FLAC frame-count/decoder compatibility.
   - Run focused sbkit tests.
3. Polyphone consumer loop:
   - Point or extend Polyphone smoke to consume the shared/generated fixtures.
   - Verify open/roundtrip/selftest non-zero audio for SF3/FLAC.
   - Keep real sbkit candidate banks optional/local-gated.
4. EWI consumer loop:
   - Add or identify a no-audio-driver FluidSynth load/render smoke for the shared fixtures.
   - Gate SF3/FLAC by existing macros and verify unsupported configs fall back to SF2.
   - Confirm render buffer/RMS is non-zero.
5. Evidence loop:
   - Prefer running `bash scripts/smoke_cross_project_soundfont.sh` from the
     Polyphone repo for the combined local gate.
   - Update sbkit docs for producer/spec changes, EWI docs for runtime policy changes, and Polyphone docs only for editor compatibility evidence.
   - Record commands, outputs, changed files, and known gaps.
   - Update docs only where the contract or results changed.

Suggested subagents:
- explore: sbkit fixture and verification surfaces
- explore: Polyphone smoke/selftest surfaces
- explore: EWI FluidSynth no-audio render and selector surfaces
- test-engineer: fixture metadata and pass/fail matrix
- critic: SFX boundary and hidden compatibility assumptions

Stop condition:
- sbkit can generate and verify shared SF3/FLAC fixtures.
- Polyphone can consume and roundtrip them with non-zero selftest playback.
- EWI can load and render them on supported FluidSynth/SF3 configs, while unsupported configs fall back safely.
- The combined Polyphone `scripts/smoke_cross_project_soundfont.sh` gate passes, or any failure is classified with exact logs.
- SFX compatibility is documented as unresolved in the sbkit SDK spec and EWI policy unless a dedicated SFX convergence implementation is included.
```
